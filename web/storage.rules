rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow all read and write operations for all files
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
